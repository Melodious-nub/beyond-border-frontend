export const environment = {
  production: true,
  // Production API URL - Update this to match your cPanel Node.js app URL
  // Common patterns:
  // - https://api.yourdomain.com/api (subdomain setup)
  // - https://yourdomain.com:3000/api (port-based setup)
  // - https://yourdomain.com/api/api (subfolder setup)
  apiUrl: 'https://api.beyond-border.org/api'
};
